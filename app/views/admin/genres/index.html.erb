<div class = "container">
  <div class = "row">
      <%= form_with model: @genre, url: admin_genres_path  do |f| %>
         <h5>ジャンル一覧・追加</h5> 
          <div class = "form-group">
           <table class="table">
            <thead>
             <tr>
              <th> <label for="name">ジャンル名</label></th>
              <th><%= f.text_field :name %></th>
              <th> <%= f.submit "新規登録",class:"btn btn-success" %></th>
             </tr>
            </thead>
           </table>
          </div>
       <%end%>
  </div>
</div>
       
 <div class = "container">
  <div class = "row">
    <div class = "col-4">     
        <table class="table">
         <thead>
          <tr class="table-secondary">
           <th></th>
           <th>ジャンル</th>
           <th></th>
          </tr>
         </thead> 
         <tbody>
          <% @genres.each do |genre| %>
           <tr>
            <td><%= genre.name %></td>
            <td></td>
            <%= form_with model: @genres, url: admin_genres_path  do |f| %>
            <td><%= f.submit "編集する",class:"btn btn-success" %></td>
            <%end%>
           </tr>
          <%end%>
         </tbody>
       </table>
   </div>
  </div>
 </div>
    

         
    